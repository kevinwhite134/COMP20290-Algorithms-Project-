# Benchmarking

## Run benchmarks
```
cd benchmarking
javac *.java
java Benchmark
```
This prints results to the console and writes `results.csv`.

## Visualize
```
python3 plot.py
```
Reads `results.csv` and generates `results.png`.
